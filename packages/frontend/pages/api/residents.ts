// This endpoint is handled by the proxy route at /api/proxy/[...path].ts
// Delete this file - it's not needed
export default function handler() {
  return { message: 'Use /api/proxy/residents instead' };
}
